# Hướng dẫn đóng góp
Cảm ơn bạn đã quan tâm đến dự án!

## Quy trình đóng góp
1. Fork repository
2. Tạo nhánh mới: `git checkout -b feature/ten-tinh-nang`
3. Commit theo quy tắc: `feat: mô tả ngắn gọn`
4. Push lên nhánh của bạn: `git push origin feature/ten-tinh-nang`
5. Mở Pull Request về nhánh `main`

## Quy tắc commit
- `feat:` tính năng mới
- `fix:` sửa lỗi
- `docs:` tài liệu
- `test:` kiểm thử
